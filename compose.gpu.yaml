services:
  speaches:
    image: ghcr.io/speaches-ai/speaches:latest-cuda-12.6.3
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
